@model GenerationLogBook.ViewModels.BQIIELossFormViewModel
@{
    ViewBag.Title = "Energy Loss";
}

<script src="~/Scripts/bq2.js"></script>
@section scripts{
    <script type="text/javascript">
        var UserId = '@Model.UserId';
        var AuthId = '@Model.AuthId';
        var LOVUnits = @Html.Raw(Json.Encode(Model.LOVUnits));

        var LOVOutageNature = @Html.Raw(Json.Encode(Model.LOVOutageNature));
        var LOVCalculateDeration = @Html.Raw(Json.Encode(Model.LOVCalculateDeration));
        var LOVStartShut = @Html.Raw(Json.Encode(Model.LOVStartShut));

        var LOVIsLDC = @Html.Raw(Json.Encode(Model.LOVIsLDC));
        var LOVForcedGrid = @Html.Raw(Json.Encode(Model.LOVForcedGrid));
        var LOVIsGas = @Html.Raw(Json.Encode(Model.LOVIsGas));
        var LOVIsPlanned = @Html.Raw(Json.Encode(Model.LOVIsPlanned));
        var LOVOutageDeration = @Html.Raw(Json.Encode(Model.LOVOutageDeration));
        var LOVStartupRamp = @Html.Raw(Json.Encode(Model.LOVStartupRamp));

        var griddata = @Html.Raw(Json.Encode(Model.Ledger3));
        var hiddenCols = @Html.Raw(Json.Encode(Model.hiddenCols));



        //implementing requirement 2 changes
        var LOVReason = @Html.Raw(Json.Encode(Model.LOVReason));
        //implementing requirement 2 changes



        $(document).ready(function () {
            InitializeForm();
        });
    </script>
}

@{
    Html.RenderPartial("_ELossTemplate");
}




